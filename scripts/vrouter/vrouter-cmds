#Add any simple commands which can be directly executed
dropstats
top -d2 -n1 -b | head -12 
vif --list
vrfstats --dump
flow -l
mpls --dump
mirror --dump
vxlan --dump
nh --list
echo endof cmds
curl http://localhost:8085/Snh_VrfListReq?name=
sysctl -a
dmesg

lsof -i

date
curl localhost:8085
date
